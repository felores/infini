---
title: Pending Test
description: Changes implemented in the current version that still require manual verification
---

# Pending Test

- Image generation API: GPT Image generation/edit requests now use `output_format`, while DALL-E requests retain `response_format`; verify official OpenAI `gpt-image-2` generation no longer returns HTTP 400 and DALL-E compatibility remains intact.
- Multi-image editing: one reference is uploaded as `image`, while two or more references use repeated `image[]` multipart fields; verify canvas generation with two, three, and larger reference sets.
- Prompt and Agent rendering: empty third-party prompt covers no longer emit empty `src` warnings, duplicate cached tags are normalized before rendering, and Agent streaming uses stable text updates without flicker; verify home, prompt library, prompt details, and a complete Codex reply.
- Local Agent diagnostics loop: source-mode startup installs Canvas Agent dependencies via frozen lockfile, upgrades Codex to `0.144.1`; on chat request failure, displays the real server/subprocess error, Playwright records 500 response bodies, and the harness stores service logs and PIDs privately in `.harness/`; needs verification of first-startup, normal conversation, and failure info display.
- Local Agent security hardening: when importing API Base URL from a link without providing a new Key simultaneously, the old Key is cleared; needs verifying the old Key is not sent to the new address.
- Local Agent security hardening: connection addresses are restricted to `localhost`, `127.0.0.1`, and `::1`; startup credentials moved to URL fragment and cleared immediately after reading; regular requests use a token header; needs verifying auto-connect, tool confirmation, and history work normally.
- Local Agent security hardening: Windows Claude launch no longer goes through shell; local config directory and token file permissions tightened to `0700` / `0600`; needs verification on the corresponding platform.
- Canvas Agent release: source package version bumped to `0.1.1`; release workflow switched to frozen Bun lockfile, explicit build, and disabled publish scripts; after upstream npm owner publishes, update plugin MCP pin from `0.1.0` to `0.1.1`.
- Supply chain hardening: plugin MCP pinned to audited `@basketikun/canvas-agent@0.1.0`; GitHub Actions pinned to full commit SHAs; needs verifying plugin and release workflows work.
- Local Agent connection instructions: new reminder that only installing the Codex plugin or manually adding MCP increases Codex token consumption; running the current repo source directly does not install MCP; needs verifying the message placement is clear.
- Local Agent connection instructions: canvas panel and config page Codex Tab now distinguish between "launch canvas via Codex plugin" and "connect from web after running Agent directly"; needs verifying copy and layout are clear.
- Site-wide Agent: Agent panel lifted from canvas page to a site-wide persistent panel mounted on the global layout right side; toggling only pushes left content without overlapping; top nav and canvas toolbar each have a toggle button; needs verifying the panel opens on home, image, video, assets, prompts, canvas pages and maintains SSE connection.
- Site-wide Agent: new `site_navigate` tool lets Agent jump directly to `/`, `/canvas`, `/canvas/:id`, `/image`, `/video`, `/prompts`, `/assets`, `/config`; navigation executes directly without tool confirmation; needs verifying jumps are correct and panel persists.
- Site-wide Agent: canvas tools (canvas_apply_ops etc.) are only available when on a canvas page; calling them off-canvas returns "Not currently on a canvas page; use site_navigate to open a canvas first"; needs verifying Agent navigates to canvas before executing canvas operations.
- Site-wide Agent: Codex session changed to a single site-level continuous thread (no longer split by canvasId); cross-page/cross-canvas shares the same conversation context; history is this thread; local Agent workspace changed from per-canvas to a fixed `site` workspace (`~/.infinite-canvas/codex-workspaces/site`); users with `@basketikun/canvas-agent` installed need to update to the new version to take effect.
- Site-wide Agent: remove dead code for the online canvas assistant that called OpenAI responses API from the frontend (including `requestToolResponse`); Agent unifies on MCP + local Codex pipeline.
- Canvas node name: each node retains a `title` name field; default name shown above the node; double-click to edit directly; visible in node info modal and JSON.
- Canvas node toolbar: node top toolbar now shows on click-select; simple mouse enter/leave should no longer trigger frequent pop-ups.
- Canvas group nodes: child nodes inside a group respond to click and drag first; when the mouse is over a node inside a group, it should move/edit that node, not the entire group.
- Canvas group nodes: toolbar adds a "Group" node; dragging a normal node into a group area highlights the target group; releasing snaps it into the group; dragging out releases the association; dragging a group node moves all child nodes together.
- GitHub Pages: new version-tag-triggered frontend static site auto-publish workflow; needs selecting GitHub Actions in repo Pages settings and verifying route refresh fallback.
- Canvas image split: rows/columns generate draggable equal-division split lines; supports adding horizontal/vertical lines, deleting individual lines, resetting split lines, and slice count preview.
- Codex App plugin docs: new plugin installation tutorial distinguishing AI auto-install, manual install, and this-repo dev-debug install; also improved plugin README and skill docs.
- Local Codex canvas connection docs: new explanation of Canvas Agent, MCP, SSE, and browser canvas interaction principles; needs confirming doc navigation and descriptions are clear.
- Top navigation: reduce desktop top nav bar height; nav item underline and content vertical alignment need confirmation.
- Configuration and user preferences: new standalone top nav page reusing the same config panel in both page and modal; new Codex Tab showing plugin installation and local Agent startup steps; configurable local Agent address, Connect token, and tool execution confirmation toggle.
- Codex global connection: config page can connect directly to the local Canvas Agent; connection status syncs to canvas and top OpenAI status icon; after refresh, auto-reconnects using locally saved address and token; needs verifying connected, connecting, failed, disconnected, and refresh auto-reconnect states.
- Canvas Agent: when old Codex thread is unavailable after restart, it auto-creates a new session and retries; needs verifying entering canvas and sending a message directly no longer shows `thread not loaded` or `no rollout found`.
- Canvas Agent: switching canvases clears and reloads the Codex session for the current canvasId; needs verifying it no longer shows the previous canvas's conversation.
- Canvas Agent: right-side Agent panel removes website/local mode toggle; fixed to local Agent.
- Configuration and user preferences: each model channel adds OpenAI/Gemini call format selection, defaulting to OpenAI; switching default format syncs the default Base URL; channel Tab adds a compact prominent reminder that model availability must be configured in the "Models" Tab.
- Gemini call format: supports fetching Gemini model lists and using them for text Q&A, online Agent tool calls, basic image generation, and image-to-image requests.
- Gemini image generation: image workbench and canvas generation convert size config to Gemini `responseFormat.image.aspectRatio`; needs verifying 1:1, 9:16, 16:9, and custom pixel size output ratios.
- Audio and video generation: selecting Gemini call format shows an unsupported notice; must still use OpenAI-compatible channels.
- WebDAV sync: remove Next.js forwarding proxy and connection method selection; browser frontend now connects directly to WebDAV service.
- Prompt library: remove Next.js route fetching and server-side in-memory cache; browser frontend now directly fetches GitHub raw files and caches to IndexedDB.
- Prompt library: remove 404 `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts` source.
- Frontend build: remove Next.js, switch to Vite + React Router; restructure frontend directories to `pages` / `components` / `stores` / `lib` / `layouts` / `styles`; needs verifying home, image/video workbench, assets, prompts, and canvas route navigation.
- Frontend build: fix TypeScript strict-mode build errors.
- Vercel deployment: add Vercel config for `web` and `docs` sub-projects; needs separately verifying static frontend and docs site deployment.
- Docker deployment: runtime image changed from Node serve to nginx static hosting; needs verifying frontend route refresh fallback.
- Environment variables: remove expired backend `.env.example` file.
- Image workbench: fix failed image retry result only saved in page state and lost after refresh or navigation; retry success result now writes to local image storage and generation records.
- Video generation: when task query endpoint returns `url` / `result_url` / `video_url` directly, skip `/content` download and save to browser local media storage first.
- Video generation: on task creation or query failure, parse the endpoint's `msg` / `message` / nested JSON error and display the real cause in the generation result failure card.
- Canvas generation config: fix model selection still showing image model (gpt-image-2) when switching to text/video/audio mode; mode switch should auto-match the default model for the corresponding capability.
